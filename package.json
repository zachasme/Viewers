{
  "scripts": {
    "start": "cd OHIFViewer && cross-env METEOR_PACKAGE_DIRS=\"../Packages\" meteor",
    "build": "cd OHIFViewer && cross-env METEOR_PACKAGE_DIRS=\"../Packages\" meteor build ../BUILD",
    "builduntar": "cd OHIFViewer && cross-env METEOR_PACKAGE_DIRS=\"../Packages\" meteor build --directory ../BUILD",
    "deploy": "cd BUILD && tar -xzf OHIFViewer.tar.gz && cd bundle/programs/server && npm install && cd ../..",
    "rundeployment": "cd BUILD/bundle && nohup sudo MONGO_URL=mongodb://localhost:27017 ROOT_URL=http://52.57.45.144 PORT=80 nodemon main.js &",
    "postinstall": "cd OHIFViewer && npm install",
    "addupstream": "git remote add upstream https://github.com/OHIF/Viewers",
    "mergeupstream": "git fetch upstream && git merge upstream/master"
  },
  "devDependencies": {
    "cross-env": "^5.1.0"
  },
  "dependencies": {
    "nodemon": "^1.12.1"
  }
}
